{
  "name": "travisphillipsmusician",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "sass": "sass --style=compressed _src/scss/style.scss assets/css/style.css",
    "watch:eleventy": "npx @11ty/eleventy --serve",
    "watch:sass": "npm run sass -- --watch",
    "start": "npm-run-all sass --parallel watch:*",
    "build": "npm run sass && npx @11ty/eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "markdown-it-attrs": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.32.8"
  }
}
